@model List<Kategori>


<div class="list-group">
    @foreach (var item in Model)
    {
        <a href="#" class="list-group-item list-group-item-action active" aria-current="true">@item.Ad</a>
    }

</div>
